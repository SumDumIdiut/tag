# Archived multiplayer code

This folder holds the networking/server code and pixel-art assets removed from the active project when it was pivoted to a single-player Celeste-movement sandbox. Nothing here is deleted — if multiplayer is wanted again later, these files can be copied back into `game/`.
